import { ShownField } from '../GlobalTypes';
export declare function shown<T extends {
    shown?: ShownField;
}>(one: T): boolean;
export declare function getAllShownItems<T extends {
    shown?: ShownField;
}>(items: T[]): T[];
