import { Store } from 'redux';
import { IDockingOptions } from "redux-openfin/docking";
import { IRootState } from '../reduxs';
declare const _default: (fin: any, finUuid: string, sharedActions: string[], enableAutoDocking: boolean, dockingOptions: Partial<IDockingOptions>, parentRootState?: IRootState) => Store<any, import("redux").AnyAction>;
export default _default;
