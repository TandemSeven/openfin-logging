import { Action } from 'redux-actions';
/**
 * async wrapper for api v2
 *
 *
 * @param action
 * @param resActionCreator
 * @param finCb
 */
export default function wrapAsyncFun<RequestPayloadType, ResponsePayloadType>(action: Action<RequestPayloadType>, resActionCreator: (options: ResponsePayloadType) => Action<ResponsePayloadType>, finCb: (fin: any) => Promise<Action<ResponsePayloadType>>): Promise<Action<ResponsePayloadType>>;
