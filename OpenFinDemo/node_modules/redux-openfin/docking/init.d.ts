import { FinWindow } from '../GlobalTypes';
import { IDockingOptions, IRectangle } from './DockingType';
import LocalStoragePersistence from './LocalStoragePersistence';
import DockingManager from './DockingManager';
export declare type State = {
    monitors: IRectangle[];
    persistenceService: LocalStoragePersistence;
    options: IDockingOptions;
    dockingManager: DockingManager;
};
export declare const initState: State;
declare const _default: (fin: any, mainFinWindow: FinWindow, dockingOptions: Partial<IDockingOptions>) => void;
export default _default;
