import { Action } from 'redux-actions';
import * as types from "./types";
export declare function publish(action: Action<types.PublishPayload>): Promise<Action<types.PublishResPayload>>;
export declare function subscribe(action: Action<types.SubscribePayload>): Promise<Action<types.SubscribeResPayload>>;
